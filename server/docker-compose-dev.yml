version: '3'

services:
  app:
    build:
      context: ../..
      dockerfile: server/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ./server:/app
    environment:
      - SPRING_PROFILES_ACTIVE=development
